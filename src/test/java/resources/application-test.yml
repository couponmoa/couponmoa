spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: localhost
    port: 2525
    username: dummy
    password: dummy

jwt:
  secret:
    key: "mL03sk8iqOnCQ6/45LTBRRMepx/AVTuZzDPiR0DDHyM="

cloud:
  aws:
    sqs:
      queue:
        email-alert: dummy-url